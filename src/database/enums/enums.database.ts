

export enum TransactionStatus {
    pending='pending',
    success='success',
    failed='failed'
}

export enum VendorDecision {
    accepted='accepted',
    declined='declined',
    pending='pending'
}